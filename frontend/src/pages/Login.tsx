import React from 'react';
import LoginForm from '../components/LoginForm';

/**
 * Login Page - Orquestador de la página de autenticación
 * Sigue el patrón: pages/ orquesta → components/ implementa
 */
const LoginPage: React.FC = () => {
  return <LoginForm />;
};

export default LoginPage;