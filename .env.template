# Environment Configuration Template for Validador de Instrumentos
# Copy this file to .env and fill in the values
# NEVER commit .env to version control

# ============================================
# CRITICAL SECURITY VARIABLES
# ============================================

# Flask secret key - GENERATE WITH: openssl rand -base64 32
# Must be at least 32 characters long for production
SECRET_KEY=

# Institutional access key - GENERATE WITH: openssl rand -base64 24
# This is the key users will use to access the application
INSTITUTIONAL_ACCESS_KEY=

# ============================================
# ENVIRONMENT CONFIGURATION
# ============================================

# Environment: development, production
# CRITICAL: Must be 'production' for production deployment
FLASK_ENV=production

# Frontend URL for CORS configuration
# Development: http://localhost:3000
# Production: https://yourdomain.com
FRONTEND_URL=

# Database file path
DATABASE_PATH=validador.db

# ============================================
# OPTIONAL CONFIGURATION
# ============================================

# Server port (default: 5000)
# PORT=5000

# Upload directory (default: uploads)
# UPLOAD_FOLDER=uploads

# Maximum file upload size in bytes (default: 16MB)
# MAX_CONTENT_LENGTH=16777216

# ============================================
# LOGGING CONFIGURATION (OPTIONAL)
# ============================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# Log file path (leave empty for console logging)
# LOG_FILE=app.log

# ============================================
# SECURITY NOTES
# ============================================

# 1. PRODUCTION REQUIREMENTS:
#    - SECRET_KEY must be at least 32 characters
#    - INSTITUTIONAL_ACCESS_KEY must be at least 16 characters
#    - FLASK_ENV must be 'production'
#    - FRONTEND_URL must be the exact production domain
#    - Never use development keys in production

# 2. DEVELOPMENT SETUP:
#    - Can use simpler keys for local development
#    - FLASK_ENV should be 'development'
#    - FRONTEND_URL should be 'http://localhost:3000'

# 3. KEY GENERATION COMMANDS:
#    SECRET_KEY: openssl rand -base64 48 | tr -d "=+/" | cut -c1-32
#    INSTITUTIONAL_KEY: openssl rand -base64 32 | tr -d "=+/" | cut -c1-24

# 4. SECURITY CHECKLIST:
#    □ .env file has 600 permissions (chmod 600 .env)
#    □ .env is in .gitignore
#    □ Production keys are unique and secure
#    □ HTTPS is configured in production
#    □ Firewall is properly configured
#    □ Regular backups are scheduled